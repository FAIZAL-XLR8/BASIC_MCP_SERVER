[project]
name = "mcp-expense"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FAIZAL-XLR8", email = "slif858@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=4.0.4",
]

[project.scripts]
mcp-expense = "mcp_expense:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
